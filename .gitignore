# Secrets — never commit these
.env
credentials.json
token.json

# Temporary processing files
.tmp/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# Env backups (full copies of .env with live keys; '.env' does not match them)
.env.bak*
.env.*.bak

# OneDrive folder-metadata files (created everywhere, never source)
desktop.ini
**/desktop.ini
